#ifndef MAIN_H
#define MAIN_H

/* This is a "header guard". It prevents this file from being included more than once
   during a single compilation, which is good practice and would have also
   prevented the infinite loop. */

// --- DECLARATIONS ---
// We declare that a function named engine_init exists somewhere.
// The actual code for it is in main.c.
void engine_init();

#endif // MAIN_H
